server_url: https://nuage.example.com
token: your-api-token-here
sync_dir: ~/Nuage
poll_interval: 30
# space: 1                   # optional; `nuage spaces use <name-or-id>` writes it
ignore_patterns:
  - ".DS_Store"
  - "*.tmp"
  - ".nuage/"
  - "Thumbs.db"
  - ".git/"
